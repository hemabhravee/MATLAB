%(a)
    clear all;
    close all;
    clc;

%(b)
    %prompt = 'Enter Value';
    %x = input(prompt);
    %y = x*10;

    %[a,b] = ginput(2) 
    %stores vectors
    %a contains the values of one axis
    %b contains the values of another axis

%(c)
    %disp(x)  
    %displays variable value without it's name
    
    %format = 'X = %f \nY = %f \n';
    %fprintf(format,x,y);
    %This prints the variables in between the string(formatSpec)
    
    %format = '%f%f';
    %str = sprintf(format,x,y);
    %What fprintf would have printed, sprintf stores in a string
    
    %n1 = 12.23;
    %s = int2str(n1);
    %disp(s);
    
    %n1 = 12.43;
    %s = num2str(n1);
    %disp(s);
    
    
    
    %With loops and if-else, we need to use 'end' keyword to specify where
    %the blocks end
    %i=10;
    %while 1
    %    if i > 20
    %        break;
    %    else
    %        i = i+1;
    %        disp(i);
    %    end
    %end
    
    %'exit' keyword closes the matlab so don't use it unless necessary
    %exit
    
    %doublemin = realmin;
    %singlemin = realmin('single');
    %doublemax = realmax;
    %singlemax = realmax('single');
    %format = '%f\n%f\n%f\n%f';
    %fprintf(format,doublemin,singlemin,doublemax,singlemax);
    
    
    
    
    
    
    
    
    
    
    